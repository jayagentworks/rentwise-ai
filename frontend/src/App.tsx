import { useState } from 'react'; import { Building2 } from 'lucide-react'; import { SearchWizard } from './components/SearchWizard'; import { Results } from './components/Results'; import type { Preferences,SearchResponse } from './types'; import './styles.css'; import './helper.css';
export default function App(){const [lang,setLang]=useState<'zh'|'en'>('zh');const [data,setData]=useState<SearchResponse|null>(null);const [loading,setLoading]=useState(false);async function search(p:Preferences){setLoading(true);try{const res=await fetch('/api/search',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(p)});if(!res.ok)throw new Error('Search failed');setData(await res.json())}catch(e){alert(lang==='zh'?'后端暂时不可用，请确认服务已启动。':'Backend unavailable.')}finally{setLoading(false)}}return <><a className="skip" href="#main">Skip</a><header><a className="brand" href="/"><Building2/><span>Rent<span>Wise</span></span></a><div className="mode"><span>{data?'02 / DECIDE':'01 / DEFINE'}</span><button onClick={()=>setLang(lang==='zh'?'en':'zh')}>{lang==='zh'?'EN':'中文'}</button></div></header><div id="main">{data?<Results data={data} lang={lang} onReset={()=>setData(null)}/>:<SearchWizard onSubmit={search} loading={loading} lang={lang}/>}</div><footer><span>RentWise AI</span><p>{lang==='zh'?'基于证据的租房决策，不替代线下核验与专业意见。':'Evidence-led rental decisions. Always verify offline.'}</p></footer></>}
